// Create a function that takes an array of strings and returns a function that,
// when called, logs each string to the console. The function should "remember" which
// strings have already been logged, and only log the remaining strings when it's called again.

function a(arrStrings){
    return b;
}
