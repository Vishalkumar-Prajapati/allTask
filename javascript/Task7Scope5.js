// Declare a constant variable
const a = 50;

// Try to reassign the variable (this will throw an error)
// a = 500; //TypeError: Assignment to constant variable.

// Modify the value of the variable
const b = 5;
const x = a * b ;
console.log(x);